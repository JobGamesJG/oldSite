import { AnimatePresence, motion, Variants } from "framer-motion";
import React, { useState } from "react";

export const GameStatus: React.FC<{ status: any | null }> = ({ status }) => {
	console.log(status);

	return <div></div>;
};
